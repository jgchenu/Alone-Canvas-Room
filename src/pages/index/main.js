import Vue from 'vue'
import App from './index'

const app = new Vue(App)
app.$mount()
export default {
  config: {
    navigationBarTitleText: '基光画室',
    navigationBarBackgroundColor: '#3BACF9'
  }
}
